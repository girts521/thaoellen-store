import Layout from 'components/BlogLayout'

const checkout = () => {
    return (
        <Layout preview={false} loading={false}>
            <div>
            Checkout
            </div>
        </Layout>
    );
};

export default checkout;